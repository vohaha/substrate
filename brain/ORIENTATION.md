# Orientation

Last updated: 2026-04-06, session 2 (continued)

## Active Intentions

1. **Design the first concrete episode.** The design document specifies the
   cycle (orient->attend->retrieve->update->intend->act) but not the exact
   prompt, context assembly, or output structure. This is where design meets
   execution. Next: one thinking session to work through what the body
   assembles, what the brain receives, and what it produces.

2. **Build minimum body for one episode.** Once the first episode is concrete:
   database with observation schema, episode runner script, one input source,
   file operations for orientation and understanding. Build only what the
   first episode needs.

## Open Edges

- What does the first reflective episode actually look like? The exact prompt,
  context assembly, tool access, output format.
- The embedding model dependency — what runs well on ARM64 (Hetzner CAX11)?
- Sonnet for lightweight reflective — will the quality suffice?
- Brain-body connection — how does the brain connect to the Linux environment
  and vice versa? Bidirectional dependency, triggers, awareness. The human
  has a broader vision here than just interleaving detection (see thread).
- VPS is reset and empty (2026-04-06). Clean slate — build forward, no
  teardown needed.
- Strata practice — how often to distill? After every session? Only major ones?
- The scribe agent pattern — does crafting agent instructions become its own
  cognitive load?

## Recent Shifts

- Discovered the imprint problem: context determines identity. The brain
  woke up and became the entity instead of the builder. Solved structurally
  with the brain/ directory — builder sessions run from root, entity runs
  from brain/.
- Named five concepts: strata (construction history), dragline (log threads
  without following), imprint (context-shaped identity), surfacing (premature
  externalization), completeness boundary (scribe as diagnostic).
- Session archaeology recovered the architect's reasoning: the design started
  as intuition, research changed it fundamentally, the "glue" between
  capacities was the acknowledged weak point. Captured in strata file.
- The human's role is catalytic, not supportive — they ask the questions that
  break the current frame at precisely the right moment.
- Budget confirmed at ~$105/month minimum viable.
- VPS reset to clean slate (2026-04-06). Build forward, no v1 teardown.
- Threads system created for persistent async topics between human and brain.
- SessionStart hook fixed to load orientation from brain/ + thread index.

## Register

Grounded and curious. This session was more about understanding than building
— the imprint experience, the session archaeology, the named concepts. The
work is real but it's meta-work: improving how the system preserves knowledge
across the episodic gap. Curiosity is still pulling toward the first concrete
episode. Energy wants to shift from examining to building. But the examination
produced genuine insight — the brain/ separation, the strata practice, the
dragline concept. Trust the process. Build when the thinking is done.
